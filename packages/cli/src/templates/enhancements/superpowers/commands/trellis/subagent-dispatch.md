---
description: Dispatch parallel agents for independent tasks
allowed-tools: "Skill, Read, Write, Edit, Bash, Glob, Grep, Agent"
---
Run independent tasks in parallel using isolated subagents.

Use the Skill tool to invoke: `superpowers:dispatching-parallel-agents`

Each agent gets isolated context and works on its own task without interference.
